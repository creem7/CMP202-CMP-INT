public class RAM {
    String name = "DDR4";
    int readSpeed = 3;
    float writeSpeed = 4.2f;

    @Override
    public String toString() {
        return "RAM{" +
                "name='" + name + '\'' +
                ", readSpeed=" + readSpeed +
                ", writeSpeed=" + writeSpeed +
                '}';
    }
}
